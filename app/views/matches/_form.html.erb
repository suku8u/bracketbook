<%= form_for [@bracket, @match] do |f| %>
  <% if @match.errors.any? %>
    <div id="error_explanation" class="alert-block alert-danger">
      <a href="#" class="close" data-dismiss="alert">&#215;</a>
      <p><%= pluralize(@match.errors.count, "error") %> prohibited this match from being saved:</p>
      <ul>
      <% @match.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="well">
    <div class="field">
      <%= f.label :team1_score, Team.find(@match.team1_id).name %>
      <%= f.number_field :team1_score %>
    </div>
    <div class="field">
      <%= f.label :team2_score, Team.find(@match.team2_id).name %>
      <%= f.number_field :team2_score %>
    </div>
    <div class="actions">
      <%= f.submit :class => "btn btn-success" %>
    </div>
  </div>
<% end %>
